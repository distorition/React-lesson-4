import { grey } from "@mui/material/colors";
import { createTheme } from "@mui/system";

const blueTheme=createTheme({
    palette:{
        mode:'blue',
        primary:{
            main:'#42a5f5'
        }
    }
})